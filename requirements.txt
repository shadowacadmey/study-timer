# ── پکیج‌های اصلی Study Timer Pro ─────────────────────────────
customtkinter>=5.2.2      # رابط کاربری مدرن
pystray>=0.19.5           # سینی سیستم ویندوز
Pillow>=10.0.0            # ساخت آیکون سینی
edge-tts>=7.2.0           # تبدیل متن به گفتار فارسی (Microsoft Edge TTS)

# ── اختیاری ────────────────────────────────────────────────────
# playsound>=1.3.0        # فقط اگر می‌خواهید فایل‌های MP3 پخش شوند
# pyinstaller>=6.0        # فقط برای ساخت فایل exe مستقل
